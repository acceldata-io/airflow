# Apache Airflow Build Requirements
# Airflow Version: 2.8.3
# Python Version: 3.14
# Constraints: Modified from https://raw.githubusercontent.com/apache/airflow/constraints-2.8.3/constraints-3.14.txt

# Build tools
setuptools
docutils
pytest-runner
Cython

# Use PyMySQL instead of mysqlclient (pure Python, no C compilation needed)
PyMySQL==1.1.3

# Pin versions to match production Python 3.14 environment
# 23.2.1 (not 24.x): gcloud-aio-storage via providers-google needs <24.0.0; cncf-kubernetes needs >=23.2.0
aiofiles==23.2.1
cachetools==7.1.4
distlib==0.3.9
filelock==3.29.0
google-auth==2.53.0
kubernetes==35.0.0
kubernetes-asyncio==35.0.1
platformdirs==4.9.6
requests-oauthlib==2.0.0
websocket-client==1.8.0

# Core Airflow with specific extras (NOT using [all] to avoid mysql extra pulling mysqlclient)
apache-airflow[celery,cncf.kubernetes,ldap,kerberos,statsd,openlineage,postgres,redis,ftp,http,imap,sqlite,async,crypto,password]==3.2.2

# Swagger UI support
connexion[swagger-ui]

# Kubernetes provider (matching existing env)
apache-airflow-providers-cncf-kubernetes==10.17.1

# xDP providers moved into ODP base packaging (ODP-7358).
# Versions match the upstream 2.8.3 constraints, so no constraint overrides needed.
apache-airflow-providers-apache-hdfs==4.3.3
apache-airflow-providers-amazon==8.19.0
apache-airflow-providers-google==10.16.0
apache-airflow-providers-microsoft-azure==9.0.1
authlib==1.3.0

# Data processing
numpy
pandas

# Environment packing
virtualenv
venv-pack
