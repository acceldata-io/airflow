# Apache Airflow Build Requirements
# Airflow Version: 2.8.3
# Python Version: 3.11
# Constraints: Modified from https://raw.githubusercontent.com/apache/airflow/constraints-2.8.3/constraints-3.11.txt

# Build tools
setuptools
docutils
pytest-runner
Cython==3.2.4

# Use PyMySQL instead of mysqlclient (pure Python, no C compilation needed)
PyMySQL==1.1.1

# Pin versions to match production Python 3.11 environment
aiofiles==24.1.0
cachetools==5.5.2
distlib==0.3.9
filelock==3.20.3
google-auth==2.38.0
kubernetes==31.0.0
kubernetes-asyncio==30.3.1
platformdirs==4.3.7
requests-oauthlib==2.0.0
websocket-client==1.8.0

# OpenLineage bump (upstream constraints pin 1.9.1)
# Provider 1.14.0 requires client >= 1.24.2, so client stack is pinned there.
openlineage-python==1.24.2
openlineage-integration-common==1.24.2
openlineage-sql==1.24.2

# Provider bump to get modern AirflowDagRun facet, hook-level lineage, fileloc, etc.
# Provider >= 1.9 requires apache-airflow-providers-common-compat.
# Provider >= 1.14 requires common-sql >= 1.20.0, and common-sql 1.20.0 needs sqlparse >= 0.5.1.
apache-airflow-providers-openlineage==1.14.0
apache-airflow-providers-common-compat==1.2.2
apache-airflow-providers-common-sql==1.20.0
# 0.5.4 per OSV-16696 CVE bump (constraints-3.11.txt); still satisfies common-sql >= 0.5.1
sqlparse==0.5.4

# Core Airflow with specific extras (NOT using [all] to avoid mysql extra pulling mysqlclient)
apache-airflow[celery,cncf.kubernetes,ldap,kerberos,statsd,openlineage,postgres,redis,ftp,http,imap,sqlite,async,crypto,password]==2.8.3

# Swagger UI support
connexion[swagger-ui]

# Kubernetes provider (matching existing env)
apache-airflow-providers-cncf-kubernetes==10.0.1

# xDP providers moved into ODP base packaging (ODP-7358).
# Versions match the upstream 2.8.3 constraints, so no constraint overrides needed.
apache-airflow-providers-apache-hdfs==4.3.3
apache-airflow-providers-amazon==8.19.0
apache-airflow-providers-google==10.16.0
apache-airflow-providers-microsoft-azure==9.0.1
authlib==1.3.0

# Data processing
numpy
pandas

# Environment packing
virtualenv
venv-pack
