# Additional Dependencies for Source-Built Airflow Tarball
# Note: apache-airflow is NOT included here - it's installed from local source
# Python Version: 3.8
# Versions aligned with requirements.txt and Airflow 2.8.1 constraints-3.8.txt

# Use PyMySQL instead of mysqlclient (pure Python, no C compilation needed)
PyMySQL==1.1.1

# Pin google-re2 to 1.1 (last version compatible with Python 3.8, uses pybind11)
google-re2==1.1

# Pin versions to match production Python 3.8 environment (from requirements.txt)
aiofiles==24.1.0
cachetools==5.5.2
distlib==0.3.9
filelock==3.16.1
google-auth==2.38.0
kubernetes==30.1.0
kubernetes-asyncio==30.1.0
platformdirs==4.3.6
requests-oauthlib==2.0.0
websocket-client==1.8.0

# Swagger UI support
connexion[swagger-ui]==2.14.2

# Kubernetes provider (matching existing env)
apache-airflow-providers-cncf-kubernetes==8.4.2

# Data processing (pinned to last Python 3.8 compatible versions)
numpy==1.24.4
pandas==2.0.3

# XML processing and security (from requirements.txt)
lxml==5.3.1
xmlsec==1.3.14

# Markdown/doc rendering (used by Airflow web UI for DAG docs)
Markdown==3.5.2
docutils==0.20.1

# Data validation (from Airflow 2.8.1 constraints-3.8)
pydantic==2.5.3

# Retry logic (from Airflow 2.8.1 constraints-3.8)
backoff==2.2.1

# Environment packing
virtualenv==20.25.0
venv-pack
